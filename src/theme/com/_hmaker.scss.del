.hmaker-site-manager {
  //---------------------------------------------
  .site-current-view {
    $cvH : SZ(40);
    //-------------------------------------------
    padding-top: $cvH;
    //-------------------------------------------
    &.is-changed {
      .current-view-title {
        > .as-name {
          @include color($color-secondary, c);
        }
      }  
    }
    //-------------------------------------------
    .current-view-title {
      @include pos-abs($t:0,$l:0,$r:0,$h:$cvH);
      @include flex-align-nowrap($ai:center);
      @include border(1px, solid, shallow, bottom);
      padding: 0 SZ(6);
      //-----------------------------------------
      > .as-name {
        @include flex-align-nowrap;
        @include font-fixed;
        flex:0 0 auto;
        > .as-icon {margin:0 SZ(6)}
      }
      //-----------------------------------------
      > .as-crumb {
        flex:1 1 auto;
      }
      //-----------------------------------------
      > .as-actions {
        flex:0 0 auto;
      }
      //-----------------------------------------
    }
    //-------------------------------------------
    .current-view-con {
      position: relative;
    }
    //-------------------------------------------
  }
  //---------------------------------------------
}
/////////////////////////////////////////////////
.hmaker-site-node-editing {
  //---------------------------------------------
  $headH : SZ(37);
  //---------------------------------------------
  position: relative;
  padding:$headH 0 0 0;
  //---------------------------------------------
  > .as-head {
    @include pos-abs($t:0,$l:0,$r:0,$h:$headH);
    @include border(1px, solid, shallow, bottom);
    @include color($color-bar, r, c);
    @include flex-align-nowrap;
    @include noselect;
    padding:0 SZ(8);
    //-------------------------------------------
    > .at-left {
      @include flex-align-center;
      flex:1 1 auto; font-size:SZ(14);
      > i {font-size: 1.2em;}
      > span {margin:0 SZ(6);}
    }
    //-------------------------------------------
  }
  //---------------------------------------------
  > .as-edit {
    @include pos-abs($b:0,$l:0,$r:0,$t:$headH);
  }
  //---------------------------------------------
}
/////////////////////////////////////////////////
.hmaker-edit-site-nav {
  //---------------------------------------------
  $formH : SZ(260);
  //---------------------------------------------
  position: relative;
  padding:0 0 $formH 0;
  //---------------------------------------------
  > .as-table {
  }
  //---------------------------------------------
  > .as-form {
    @include pos-abs($b:0,$l:0,$r:0,$h:$formH);
    @include border(1px, solid, shallow, top);
  }
  //---------------------------------------------
}
/////////////////////////////////////////////////
.hmaker-edit-com {
  $ctH : SZ(42);
  $pad : SZ(4);
  //---------------------------------------------
  position: relative;
  padding-top: $ctH;
  //---------------------------------------------
  > .as-com-type {
    @include pos-abs($t:0, $l:0, $r:0, $h:$ctH);
    padding:$pad;
  }
  //---------------------------------------------
  > .as-com-conf {
    width:100%; height:100%;
    padding:0 $pad $pad $pad;
    > .as-con {
      @include border(1px, solid, shallow);
      width:100%; height:100%;
      > .ti-form > .form-body {padding:0;}
    }
  }
  //---------------------------------------------
}
/////////////////////////////////////////////////